# profile

Owns durable profile changes and authenticated profile retrieval.

`profile_update` classifies restrictions and preferences. `recuperar_perfil` reads the remote
Victus profile. Both are catalog entries executed through the shared runtime.
